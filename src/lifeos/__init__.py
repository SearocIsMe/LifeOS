"""LifeOS Phase 0 kernel package.

Restored 2026-09-16 (K8s migration, HANDOVER §8.6 repo-integrity check): the
original file never entered git because `.gitignore`'s `_*.*` pattern matched
`__init__.py`; the version constants below are taken verbatim from the frozen
Gate 0 report (phases/phase-0/reports/gate0_report.json, 2026-09-15).
"""

SCHEMA_VERSION = "0.1.0"
POLICY_VERSION = "0.1.0"
RULES_VERSION = "0.1.0"
